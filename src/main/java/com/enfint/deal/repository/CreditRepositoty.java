package com.enfint.deal.repository;

import com.enfint.deal.entity.Credit;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CreditRepositoty extends JpaRepository<Credit,Long> {
}
